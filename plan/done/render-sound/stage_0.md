# Этап 0 — зелёная база

Перед правками зафиксировать исходное состояние.

1. Плагин `@vimp-games/tanks` слинкован в `node_modules` движка (и обратно —
   `npm link` в обе стороны, dev отдаёт `/@fs/` исходники плагина).
2. В `/Users/dmitry/Sites/my/vimp`: `npx eslint .`, `npm test`,
   `npm run core:test`.
3. В `/Users/dmitry/Sites/my/vimp-tanks`: те же три команды.

Все шесть прогонов должны быть зелёными до начала этапа 1; иначе разбираемся
с падениями отдельно, чтобы не смешивать их с новыми.
